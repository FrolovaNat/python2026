Время затраченное на выполнение: 0:33

result: 60/100

1) **Сильные стороны**
- Студент предоставил SQL-запросы для всех девяти подзадач, что показывает понимание структуры задания.
- В большинстве запросов правильно используются базовые операторы SELECT, FROM, WHERE.
- Запросы компактны и не содержат излишнего кода.

2) **Ошибки и недочёты (сгруппировать по серьёзности)**

**Блокирующие (ломает выполнение требований задания)**
- **Задача 1.3 (Уровень 1):** Требовалось получить список имен всех женщин, которые путешествовали первым классом. В решении `SELECT name, Pclass FROM titanic WHERE sex = 'female' or Pclass = 1;` используется оператор `OR`, что возвращает имена всех женщин ИЛИ всех пассажиров первого класса, а не только женщин первого класса. Это грубая логическая ошибка, не соответствующая условию. Исправление: `SELECT name FROM titanic WHERE sex = 'female' AND Pclass = 1;`
- **Задача 3.3 (Уровень 3):** Требовалось выбрать пассажиров, которые заплатили за билет меньше 10, но путешествовали не третьим классом. В решении `SELECT name, Fare, Pclass FROM titanic WHERE Pclass<10 and Pclass !=3;` отсутствует условие на стоимость билета (`Fare < 10`). Вместо него стоит бессмысленное условие `Pclass<10` (класс всегда 1, 2 или 3). Это полностью искажает логику запроса. Исправление: `SELECT name, Fare, Pclass FROM titanic WHERE Fare < 10 AND Pclass != 3;`

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- **Задача 2.1 (Уровень 2):** Требовалось вывести имена и возраст выживших пассажиров. В решении `SELECT name, age, Survived FROM titanic WHERE survived = 1 ORDER BY Age;` в выборку добавлена лишняя колонка `Survived`. Хотя это не приводит к ошибке данных, это отклонение от явного требования вывода только двух колонок ("имена и возраст"). Исправление: `SELECT name, age FROM titanic WHERE survived = 1 ORDER BY Age;`
- **Задача 2.2 (Уровень 2):** Требовалось вывести имена пассажиров, которые путешествовали одни (без братьев, сестёр, супруга или супруги, родителей или детей). В решении `SELECT name from titanic WHERE SibSp=0 and Parch=0;` логика верна, но в условии задачи в псевдокоде указана проверка также количества супругов. В реальной таблице `titanic` колонка `SibSp` объединяет братьев/сестёр и супругов, поэтому запрос корректен. Однако, для полного соответствия текстовому описанию "без братьев, сестёр, супруга или супруги, родителей или детей" запрос верен. Недочёт незначительный, но можно отметить как частичное соответствие формулировке.
- **Общий стиль:** Отсутствует явное разделение запросов на уровни (например, комментариями). Это ухудшает читаемость и проверку соответствия каждого запроса своей задаче.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- **Форматирование:** В первом запросе `select*from titanic;` отсутствуют пробелы, что снижает читаемость. Рекомендуется: `SELECT * FROM titanic;`
- **Согласованность:** В разных запросах используется разный регистр для имен колонок (`Name` vs `name`). В SQL это часто не критично, но для стиля лучше быть последовательным.
- **Избыточный вывод:** В задаче 2.1, как отмечено выше, вывод колонки `Survived` избыточен.

3) **Оценка и как она посчитана**

- **Функциональность и соответствие условию: 25/50**
  - Минус 15 за две блокирующие ошибки (задачи 1.3 и 3.3), которые полностью меняют смысл запросов и не соответствуют условию.
  - Минус 10 за значимые недочёты: отклонение от требуемого списка колонок в задаче 2.1 и неидеальное соответствие текстовому описанию в задаче 2.2 (хотя логически верно).
- **Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 20/30**
  - Минус 5 за плохое форматирование и отсутствие разделения запросов на уровни, что ухудшает читаемость.
  - Минус 5 за несогласованность в стиле (регистр имен колонок).
- **Стиль и тесты: 15/20**
  - Минус 5 за отсутствие комментариев или структурирования, которые прямо не требовались, но сильно улучшили бы понимание решения.

**Итог:** 25 + 20 + 15 = 60 баллов.

4) **Если задание выполнено не полностью**
Задание выполнено полностью по количеству запросов, но два из них содержат критичные логические ошибки, а один — отклонение от требуемого формата вывода.

**Вариант исправленного решения (ключевые исправления):**
```sql
-- Уровень 1
-- 1.1
SELECT * FROM titanic;

-- 1.2
SELECT Name, Age FROM titanic WHERE Age > 30;

-- 1.3
SELECT Name FROM titanic WHERE Sex = 'female' AND Pclass = 1;

-- Уровень 2
-- 2.1
SELECT Name, Age FROM titanic WHERE Survived = 1 ORDER BY Age;

-- 2.2
SELECT Name FROM titanic WHERE SibSp = 0 AND Parch = 0;

-- 2.3
SELECT Name, Pclass FROM titanic WHERE Fare > 100;

-- Уровень 3
-- 3.1
SELECT Name, Pclass, Age FROM titanic WHERE Pclass != 1 AND Age > 18;

-- 3.2
SELECT * FROM titanic WHERE Survived = 0 AND SibSp = 0 AND Parch = 0;

-- 3.3
SELECT Name, Fare, Pclass FROM titanic WHERE Fare < 10 AND Pclass != 3;
```
