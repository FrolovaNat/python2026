Дней на выполнение: 3

result: 40/100

1) **Сильные стороны**
- Созданы таблицы с правильными полями согласно условиям каждого уровня.
- Вставлены записи с данными, соответствующими примерам.
- Выполнены операции UPDATE и DELETE для уровней 2 и 3.
- Код написан на SQL, что соответствует заданию по работе с базой данных.

2) **Ошибки и недочёты**

**Блокирующие (ломает выполнение требований задания)**
- Отсутствует выполнение запроса для получения всех записей из таблиц. В условии каждого уровня явно указано: "Выполните запрос, чтобы получить все записи из таблицы и отобразить их." В решении нет ни одного оператора `SELECT`. Это делает решение неполным и нефункциональным, так как основная цель — получение и отображение данных — не выполнена.
- В уровне 3 нарушена структура таблицы: поле названия товара должно быть `name`, но в таблице `users2` оно названо `name`, что верно, однако в условии указаны поля "название" и "цена", а в коде они названы `name` и `price`, что корректно. Но есть критическая ошибка: при вставке четвертого товара используется `id=3`, который дублирует `id` третьего товара ("Клавиатура"). Это нарушает уникальность идентификатора (даже если ограничение PRIMARY KEY не задано, логически id должен быть уникальным). В результате операция `DELETE FROM users2 WHERE id = 3;` удалит оба товара с id=3 ("Клавиатура" и "Монитор"), что не соответствует условию (должен удалиться только товар "Клавиатура" с id=3, а "Монитор" с id=4 должен остаться). Это приводит к неверному результату.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- Использование оператора `DROP TABLE` перед созданием таблиц не требуется в условии и может привести к потере данных при повторном выполнении, но не является ошибкой с точки зрения задания. Однако, если бы база данных уже содержала нужные таблицы, это могло бы быть оправдано. В контексте задания это незначительно.
- Имена таблиц (`users`, `users1`, `users2`) не соответствуют семантике задания (студенты, сотрудники, товары). Условие прямо указывает на создание таблиц для конкретных сущностей. Это ухудшает читаемость и соответствие условию, хотя не ломает функциональность.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- В SQL-коде есть лишние пробелы и неконсистентное форматирование (например, отступы перед `DROP TABLE`). Это не влияет на выполнение, но снижает читаемость.
- В уровне 3 в поле `name` таблицы `users2` хранятся названия товаров, что корректно, но в условии поле называется "название", а в коде — `name`, что приемлемо.

3) **Оценка и как она посчитана**
- Функциональность и соответствие условию: 20/50. Минус 30 баллов: отсутствие запросов `SELECT` для получения и отображения записей (-25), ошибка с дублированием id в уровне 3, приводящая к неверному удалению (-5).
- Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 15/30. Минус 15 баллов: неправильные имена таблиц, снижающие понятность (-5); использование `DROP TABLE` без необходимости, хотя и не запрещено (-5); плохое форматирование кода (-5).
- Стиль и тесты: 5/20. Минус 15 баллов: тесты не предоставлены и не требовались, но код не включает вывод результатов, что критично для стиля выполнения задания (-15).

Итог: 20 + 15 + 5 = 40/100.

4) **Если задание выполнено не полностью**
- Отсутствует: выполнение запросов `SELECT` для получения всех записей из каждой таблицы и их отображение.
- Частично выполнено: операции создания, вставки, обновления и удаления выполнены, но с ошибкой в уровне 3 (дублирование id).

**Вариант полного решения (исправления и дополнения):**
Для каждого уровня нужно добавить запрос `SELECT` и исправить ошибки. Пример для уровня 1 (аналогично для других):
```sql
-- Уровень 1: Создание таблицы студентов
CREATE TABLE IF NOT EXISTS students (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    age INTEGER
);

-- Вставка записей
INSERT INTO students (id, name, age) VALUES
    (1, 'Иван', 20),
    (2, 'Мария', 22),
    (3, 'Петр', 19);

-- Получение и отображение всех записей
SELECT * FROM students;
```
Для уровня 2:
```sql
CREATE TABLE IF NOT EXISTS employees (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    position TEXT,
    salary INTEGER
);

INSERT INTO employees (id, name, position, salary) VALUES
    (1, 'Анна', 'Разработчик', 50000),
    (2, 'Олег', 'Менеджер', 60000),
    (3, 'Елена', 'Дизайнер', 45000);

UPDATE employees SET salary = 65000 WHERE id = 2;

SELECT * FROM employees;
```
Для уровня 3 (с исправлением дублирования id):
```sql
CREATE TABLE IF NOT EXISTS products (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    price INTEGER,
    quantity INTEGER
);

INSERT INTO products (id, name, price, quantity) VALUES
    (1, 'Ноутбук', 45000, 10),
    (2, 'Мышь', 1500, 50),
    (3, 'Клавиатура', 3000, 25),
    (4, 'Монитор', 12000, 15);  -- Исправлено: id=4

DELETE FROM products WHERE id = 3;

SELECT * FROM products;
```
Также рекомендуется убрать операторы `DROP TABLE`, если не требуется явное пересоздание, или использовать `CREATE TABLE IF NOT EXISTS` для избежания ошибок.
