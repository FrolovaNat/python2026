Время затраченное на выполнение: 0:21

result: 10/100

1) **Сильные стороны**
- Код демонстрирует попытку работы с файлами и использование соответствующих модулей (csv, json).
- Присутствует базовое понимание структуры данных (словари для JSON).

2) **Ошибки и недочёты**

**Блокирующие (ломает выполнение требований задания)**
- Задание состоит из трёх отдельных задач (уровней), но студент предоставил один файл с кодом, который не решает ни одну из задач полностью. Код лишь читает файлы и выводит сырые данные, а также содержит некорректные попытки записи.
- Для уровня 1: отсутствует вычисление среднего балла по каждому предмету. Код только читает CSV и печатает строки. Требуемый вывод (например, `Math: 85.0`) не генерируется.
- Для уровня 2: код читает `employees.json`, но не вычисляет сумму зарплат по отделам. Попытка записи содержит синтаксическую ошибку (двойные фигурные скобки `{{ ... }}`) и записывает статические данные в тот же файл `employees.json` (вместо создания нового файла `department_salaries.json`), перезаписывая исходные данные.
- Для уровня 3: код читает оба файла, но не выполняет расчёт общей стоимости (количество * цена) и не создаёт требуемый JSON-файл `products_total.json`. Попытка записи содержит синтаксическую ошибку (запятая как десятичный разделитель `25,0` вместо `25.0`) и записывает статические данные в файл `prices.json`, перезаписывая исходные данные.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- Во всех трёх задачах код записывает данные в те же файлы, которые читает (`employees.json`, `prices.json`), уничтожая исходные данные. Это противоречит условию (для уровня 2 требуется новый файл `department_salaries.json`, для уровня 3 — `products_total.json`).
- Использование кириллических ключей ("ИТ", "HR", "Финансы", "Яблоки", "количество", "общая_цена") не соответствует примеру вывода в условии (английские ключи: "IT", "HR", "Finance", "quantity", "total_price"). Это может привести к несоответствию формата.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- Код плохо структурирован: три задачи смешаны в одном файле без чёткого разделения (например, функций или отдельных блоков).
- Имена переменных общие (`data`, `reader`), что ухудшает читаемость.
- В JSON-данных для уровня 3 используется запятая как десятичный разделитель (`25,0`), что вызовет ошибку синтаксиса в Python (требуется точка: `25.0`).

3) **Оценка и как она посчитана**
- Функциональность и соответствие условию: 5/50 (код читает файлы, но не выполняет требуемые вычисления и вывод; серьёзные синтаксические ошибки; перезапись исходных файлов).
- Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 3/30 (код неструктурирован, содержит синтаксические ошибки, необработанные потенциальные ошибки (например, отсутствие файлов), дублирование импортов).
- Стиль и тесты: 2/20 (отсутствуют комментарии, нет тестов, нарушены базовые соглашения о форматировании).

4) **Если задание выполнено не полностью**
Задание не выполнено: отсутствуют ключевые алгоритмы вычислений и корректный вывод.

**Вариант полного решения (краткий псевдокод для каждого уровня):**

Уровень 1:
```
import csv
открыть students.csv
прочитать заголовки (первая строка)
инициализировать словарь для сумм и счётчиков по предметам
для каждой строки данных:
    для каждого предмета:
        добавить оценку к сумме предмета
        увеличить счётчик предмета
для каждого предмета:
    вывести среднее: сумма / счётчик
```

Уровень 2:
```
import json
открыть employees.json, загрузить данные
создать словарь department_totals
для каждого сотрудника:
    department = сотрудник["department"]
    salary = сотрудник["salary"]
    добавить salary к department_totals[department]
вывести department_totals
сохранить department_totals в department_salaries.json
```

Уровень 3:
```
import csv, json
загрузить prices.json в словарь prices
открыть products.csv, прочитать данные
создать словарь result
для каждого продукта в CSV:
    quantity = int(количество)
    price = prices[продукт]
    total = quantity * price
    result[продукт] = {"quantity": quantity, "total_price": total}
сохранить result в products_total.json
```
