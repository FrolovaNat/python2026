Дней на выполнение: 11

result: 65/100

1) **Сильные стороны**
- Использованы семантические теги: `header`, `main`, `section`, `article`, `footer`.
- Есть один `h1` на страницу.
- Созданы три обязательные секции: "Обо мне", "Навыки", "Проекты".
- В подвале есть контакты с минимум двумя ссылками, включая ссылку с `mailto:`.
- Изображение в шапке присутствует.
- CSS подключен через `link` в `head`.
- Применены базовые стили для `body`, `header`, `section`, `footer`.
- Использованы классы (например, `.avatar`) и селекторы по тегам.
- Есть внутренние отступы (`padding`) в карточках (`article`).

2) **Ошибки и недочёты (сгруппировать по серьёзности)**

**Блокирующие (ломает выполнение требований задания)**
- **Отсутствует `box-sizing: border-box` для всех элементов** (требование CSS п.1). В CSS нет правила `* { box-sizing: border-box; }` или аналогичного. Это может привести к неправильному расчёту размеров элементов.
- **Не выполнено требование по Flexbox** (требование CSS п.4). В коде нет использования `display: flex` для выравнивания элементов в шапке или создания сетки проектов через flex с переносом. Например, шапка не выровнена через flex, и проекты не расположены в flex-сетке.
- **Не выполнено требование по адаптивности** (требование CSS п.5). В CSS нет медиа-запросов или правил, обеспечивающих отсутствие горизонтального скролла на узком экране. Например, изображение имеет `max-width: 20%;`, что может не адаптироваться корректно, и нет ограничений ширины для контейнеров.
- **Не выполнены дополнительные элементы** (Задание 2). В условии требуется выбрать минимум 2 пункта из вариантов A-D, но в решении нет явной реализации ни одного из них (например, нет `:hover` эффектов для всех ссылок, `transition`, `::first-letter`, иконок и т.д.). Это часть обязательных требований для полного выполнения.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- **Нарушена логическая иерархия заголовков** (требование HTML п.1). В секции "Обо мне" используется `h2`, но внутри неё нет подзаголовков `h3`. Вместо этого, контент представлен как список внутри `<p>`, что семантически неверно. Также в подвале используется `h3` после `h2` в основной части, что допустимо, но структура "Обо мне" не соответствует ожидаемой иерархии.
- **Изображение в шапке не имеет атрибута `alt`** (требование HTML п.3). В теге `<img>` отсутствует атрибут `alt`, что важно для доступности.
- **В секции "Обо мне" контент некорректно вложен** (HTML структура). `<ul>` помещён внутри `<p>`, что является ошибкой валидации HTML, так как `<p>` не может содержать блочные элементы. Это может привести к непредсказуемому отображению в некоторых браузерах.
- **Не использованы разные типы селекторов CSS** (требование CSS п.2). В основном применены селекторы по тегам и один класс (`.avatar`). Нет комбинированных селекторов (например, `header h1`), что упрощает стилизацию, но не соответствует требованию.
- **Отсутствует использование псевдокласса `:hover`** (требование CSS п.2). В CSS нет правил с `:hover`, хотя это явно требуется.
- **Не использован `border-radius` для скругления** (требование CSS п.3). В CSS нет свойства `border-radius` для карточек или других элементов.
- **Изображения не адаптированы под ширину контейнера** (требование CSS п.5). Для аватара задано `max-width: 100px;`, но для общего `img` задано `max-width: 20%;`, что может не обеспечить полную адаптивность на узких экранах.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- **Опечатка в навыке "Payton"** вместо "Python". Это не влияет на функциональность, но снижает аккуратность.
- **Повторяющийся текст в проектах**. Оба проекта имеют одинаковое описание "Разработан сайт с нуля с использованием CSS", что делает контент менее информативным.
- **Недостаточная детализация в CSS**. Например, нет стилей для `:focus`, улучшенной типографики (как `line-height`), что могло бы улучшить читаемость.
- **Отсутствие комментариев или структурирования в CSS**. Код CSS можно было бы организовать лучше для удобства чтения.

3) **Оценка и как она посчитана**
- **Функциональность и соответствие условию: 25/50** (минус 25 за блокирующие ошибки: отсутствие `box-sizing`, Flexbox, адаптивности, дополнительных элементов, а также значимые ошибки: иерархия заголовков, отсутствие `alt`, некорректное вложение HTML, недостаточные селекторы CSS, отсутствие `:hover` и `border-radius`).
- **Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 20/30** (минус 10 за значимые ошибки в HTML структуре и CSS, а также минорные недочёты, такие как опечатка и повторяющийся контент).
- **Стиль и тесты: 20/20** (тесты не требовались, стиль в целом приемлем, нет грубых нарушений форматирования).

Итог: 25 + 20 + 20 = 65/100.

4) **Если задание выполнено не полностью**
- **Отсутствует**: 
  - `box-sizing: border-box` в CSS.
  - Использование Flexbox для шапки и сетки проектов.
  - Адаптивность через медиа-запросы или гибкие единицы.
  - Реализация минимум 2 дополнительных элементов из Задания 2.
  - Атрибут `alt` для изображения.
  - Корректная иерархия заголовков и вложение HTML в секции "Обо мне".
  - Псевдокласс `:hover` и `border-radius` в CSS.
- **Сделано частично**: 
  - Семантическая структура HTML в целом есть, но с ошибками.
  - Базовые стили CSS присутствуют, но не все требования выполнены.

**Вариант исправления (краткий)**:
1. В CSS добавьте в начало:
   ```css
   * {
     box-sizing: border-box;
   }
   ```
2. Для шапки используйте Flexbox:
   ```css
   header {
     display: flex;
     flex-direction: column;
     align-items: center;
   }
   ```
3. Для сетки проектов:
   ```css
   section:last-of-type {
     display: flex;
     flex-wrap: wrap;
     gap: 15px;
   }
   article {
     flex: 1 1 300px;
   }
   ```
4. Добавьте медиа-запрос для адаптивности:
   ```css
   @media (max-width: 768px) {
     img {
       max-width: 100%;
     }
     body {
       padding: 10px;
     }
   }
   ```
5. Исправьте HTML:
   - Добавьте `alt="Аватар"` в тег `<img>`.
   - Уберите `<ul>` из внутри `<p>` в секции "Обо мне".
6. Реализуйте минимум 2 пункта из Задания 2, например:
   - Добавьте `:hover` для ссылок:
     ```css
     a:hover {
       color: #007bff;
       text-decoration: underline;
     }
     ```
   - Добавьте `border-radius` для карточек:
     ```css
     article {
       border-radius: 8px;
     }
     ```
7. Исправьте опечатку "Payton" на "Python".
