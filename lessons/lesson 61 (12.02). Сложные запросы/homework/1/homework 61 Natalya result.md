Дней на выполнение: 4

result: 0/100

1) **Сильные стороны**
- База данных создана с правильным именем `University`.
- Таблицы `Students`, `Courses`, `Enrollments` созданы с корректными именами.
- Столбцы в таблицах в основном соответствуют требованиям по именам и типам данных.
- Добавлены тестовые записи в таблицы.
- Представлен SQL-запрос для вывода студентов и их курсов.
- Выполнена очистка таблиц с помощью операторов `DELETE`.

2) **Ошибки и недочёты**

**Блокирующие (ломает выполнение требований задания)**
- В таблице `Students` отсутствует столбец `StudentID` как `INTEGER`. Вместо него создан столбец `id` типа `INTEGER`. Это прямое нарушение требования 2: "Создать таблицу **Students** со столбцами: `StudentID` — уникальный идентификатор (INTEGER)." Аналогично, в таблице `Courses` вместо `CourseID` создан столбец `id`. Это нарушение требования 3. В таблице `Enrollments` вместо `StudentID` и `CourseID` созданы столбцы `student_id` и `course_id`. Это нарушение требования 4. Несоответствие имён столбцов делает невозможным корректную работу запросов, которые ожидают определённые имена полей, особенно для связей и итогового запроса.
- В таблице `Enrollments` не объявлены внешние ключи (`FOREIGN KEY`). Требование 4 гласит: "`StudentID` — ссылка на `Students.StudentID` (INTEGER). `CourseID` — ссылка на `Courses.CourseID` (INTEGER)." Это подразумевает создание ограничений внешнего ключа для обеспечения ссылочной целостности. Их отсутствие — серьёзное упущение.
- Итоговый запрос на вывод студентов и курсов (`SELECT * FROM Students JOIN Enrollments ON Students.id = Enrollments.student_id JOIN Courses ON Enrollments.course_id = Courses.id;`) использует неверные имена столбцов (`id`, `student_id`, `course_id` вместо `StudentID`, `CourseID`). Хотя при текущей структуре он выполнится, он не соответствует требованию по именам столбцов. Более критично, что в условии требуется: "Выполнить запрос, который выводит студентов и соответствующие им курсы." В рекомендациях уточняется: "выводит фамилию, имя студента и название курса". Представленный запрос выводит *все* столбцы (`SELECT *`), а не только `LastName`, `FirstName`, `CourseName`. Это не соответствует ожидаемому формату вывода.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- В таблицах `Students` и `Courses` для столбцов `id` не указано, что они являются `PRIMARY KEY`. Хотя это не строго требуется в условии, отсутствие первичного ключа для уникальных идентификаторов является плохой практикой и может привести к дублированию записей. Рекомендуемая последовательность действий также указывает на "первичный" ключ.
- Порядок очистки таблиц нарушен. В решении сначала очищается `Students`, затем `Enrollments`, затем `Courses`. Это вызовет ошибку ограничения внешнего ключа (если бы оно было объявлено), так как записи в `Enrollments` ссылаются на `Students` и `Courses`. Даже без внешних ключей, лучшей и безопасной практикой является очистка в порядке, обратном зависимостям: сначала `Enrollments` (таблица связей), затем `Students` и `Courses`. Рекомендация в задании прямо указывает: "Удалите все строки из таблиц **Enrollments**, **Students** и **Courses**" (именно в таком порядке).

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- Имена столбцов в таблицах используют разный регистр и стиль (`id`, `first_name`, `last_name` vs `name`, `credits` vs `student_id`, `course_id`). Для улучшения читаемости и согласованности лучше придерживаться единого стиля (например, CamelCase, как указано в требованиях: `StudentID`, `FirstName`).
- В INSERT-запросах можно было бы использовать более наглядные значения для имен и названий курсов.

3) **Оценка и как она посчитана**
- Функциональность и соответствие условию: 0/50. Задание не выполнено по ключевым пунктам: не созданы столбцы с требуемыми именами (`StudentID`, `CourseID` в основных таблицах и `Enrollments`), не объявлены внешние ключи, итоговый запрос выводит не те данные (`SELECT *` вместо конкретных полей). Очистка выполнена в неверном порядке.
- Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 10/30. Код структурирован, но отсутствуют важные элементы целостности (первичные и внешние ключи). Имена столбцов не соответствуют требованиям, что снижает читаемость в контексте задания. Стиль именования несогласован.
- Стиль и тесты: 10/20. SQL-запросы написаны без синтаксических ошибок, но отсутствуют комментарии. Тесты не требовались и не предоставлены. Вычет за несоблюдение формата именования и отсутствие ключевых ограничений.

Итог: 0 + 10 + 10 = 20/100. Однако, из-за блокирующих ошибок, которые делают решение несоответствующим основным требованиям, итоговая оценка должна быть 0.

4) **Если задание выполнено не полностью**
- Отсутствует: создание столбцов с именами, точно соответствующими условию (`StudentID`, `CourseID` в таблицах `Students`, `Courses` и `Enrollments`).
- Отсутствует: объявление внешних ключей (`FOREIGN KEY`) в таблице `Enrollments`.
- Частично выполнено: итоговый запрос выводит данные, но не в требуемом формате (все столбцы вместо конкретных).
- Частично выполнено: очистка таблиц выполнена, но в неверном порядке, что может привести к ошибкам при наличии ограничений.

**Вариант полного решения (исправленный SQL-скрипт):**
```sql
-- 1. Создание базы данных University (обычно делается вне SQL-скрипта, при подключении)
-- 2. Создание таблицы Students
CREATE TABLE Students (
    StudentID INTEGER PRIMARY KEY,
    FirstName TEXT NOT NULL,
    LastName TEXT NOT NULL
);

-- 3. Создание таблицы Courses
CREATE TABLE Courses (
    CourseID INTEGER PRIMARY KEY,
    CourseName TEXT NOT NULL,
    Credits INTEGER NOT NULL
);

-- 4. Создание таблицы Enrollments
CREATE TABLE Enrollments (
    StudentID INTEGER,
    CourseID INTEGER,
    FOREIGN KEY (StudentID) REFERENCES Students(StudentID),
    FOREIGN KEY (CourseID) REFERENCES Courses(CourseID)
);

-- 5. Добавление записей
INSERT INTO Students (StudentID, FirstName, LastName) VALUES
(1, 'Иван', 'Петров'),
(2, 'Мария', 'Сидорова'),
(3, 'Алексей', 'Иванов');

INSERT INTO Courses (CourseID, CourseName, Credits) VALUES
(101, 'Математика', 5),
(102, 'Физика', 4),
(103, 'История', 3);

INSERT INTO Enrollments (StudentID, CourseID) VALUES
(1, 101),
(1, 102),
(2, 101),
(3, 103);

-- 6. Запрос на вывод студентов и их курсов
SELECT Students.LastName, Students.FirstName, Courses.CourseName
FROM Students
JOIN Enrollments ON Students.StudentID = Enrollments.StudentID
JOIN Courses ON Enrollments.CourseID = Courses.CourseID;

-- 7. Очистка таблиц (в правильном порядке)
DELETE FROM Enrollments;
DELETE FROM Students;
DELETE FROM Courses;
```
