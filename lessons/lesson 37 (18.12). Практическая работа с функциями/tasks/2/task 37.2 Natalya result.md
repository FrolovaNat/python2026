result: 15/100

1. **Формулировка задания:**
   - Написать функцию `delivery_price(city, weight_kg, urgent)`, которая:
     - Возвращает `500 + 30 * weight_kg` при `urgent=True`.
     - Возвращает `300 + 20 * weight_kg` при `urgent=False`.
     - Параметр `city` пока не влияет на расчёты (задел на будущее).
   - Вызвать функцию 4 раза: дважды позиционно, дважды именованно.
   - Добавить один закомментированный вызов с ошибкой (для демонстрации исключения).

2. **Результаты проверки:**
   - **Файл:** `37.2.py`
   - **Блокирующие ошибки:**
     - Строка 2: `if urgent=True:` — синтаксическая ошибка (присваивание вместо сравнения). При запуске вызывает `SyntaxError`.
     - Строка 5: `print(delivery_price(city, weight_kg, urgent)` — отсутствует закрывающая скобка. Вызывает `SyntaxError`.
     - Строка 7: Переопределение функции `delivery_price` с другими параметрами (`text, price`) ломает исходную логику. Новая функция возвращает саму себя (`return delivery_price`), что приводит к бесконечной рекурсии при вызове.
   - **Значимые ошибки:**
     - Все вызовы функции некорректны:
       - `print(delivery_price(city, weight_kg, urgent))` — переменные `city`, `weight_kg`, `urgent` не объявлены.
       - `print(delivery_price(text, price))` и `print(delivery_price(text="яблоко", price="100"))` — используют параметры `text` и `price`, которых нет в исходной функции.
     - Отсутствует требуемый закомментированный пример ошибочного вызова.
   - **Тесты:** Код не запускается из-за синтаксических ошибок. Даже после их исправления вызовы функции используют несуществующие переменные.

3. **Сильные стороны работы:**
   - Нет корректно реализованных элементов. Код содержит критические ошибки, которые делают его неработоспособным.

4. **Ошибки:**
   - **Блокирующие:**
     1. Неправильный оператор сравнения (`=` вместо `==`).  
        **Исправление:**  
        ```python
        if urgent == True:
        # Или лучше:
        if urgent:
        ```
     2. Переопределение функции с другими параметрами.  
        **Исправление:** Удалить блок кода со строки 7 до конца.
     3. Незакрытая скобка в `print()`.  
        **Исправление:** Добавить `)` в конце строки 5.
   - **Значимые:**
     1. Неправильные вызовы функции (несуществующие переменные).  
        **Исправление:** Пример корректных вызовов:
        ```python
        # Позиционные вызовы
        print(delivery_price("Москва", 5, True))
        print(delivery_price("СПб", 3, False))
        # Именованные вызовы
        print(delivery_price(city="Казань", weight_kg=2, urgent=True))
        print(delivery_price(urgent=False, weight_kg=4, city="Владивосток"))
        ```
     2. Отсутствует пример ошибочного вызова в комментарии.  
        **Исправление:** Добавить, например:
        ```python
        # Ошибочный вызов (пропущен обязательный аргумент weight_kg):
        # delivery_price("Москва", urgent=True)
        ```

5. **Оценка:**
   - **Функциональность (50%): 0/50**  
     Код нерабочий из-за синтаксических ошибок; функция не вызывается корректно.
   - **Качество кода (30%): 10/30**  
     Переопределение функции нарушает базовые принципы читаемости.
   - **Стиль и тесты (20%): 5/20**  
     Отсутствуют комментарии, код не соответствует PEP-8 (пробелы вокруг операторов, именование).
   - **Итого: 15/100**. Снято за неработоспособность, нарушение ТЗ и низкое качество кода.

6. **Вариант исправления:**
```python
def delivery_price(city, weight_kg, urgent):
    if urgent:
        return 500 + 30 * weight_kg
    else:
        return 300 + 20 * weight_kg

# Позиционные вызовы
print(delivery_price("Москва", 5, True))
print(delivery_price("СПб", 3, False))

# Именованные вызовы
print(delivery_price(city="Казань", weight_kg=2, urgent=True))
print(delivery_price(urgent=False, weight_kg=4, city="Владивосток"))

# Пример ошибочного вызова (раскомментировать для проверки):
# delivery_price("Москва", True)  # TypeError: missing required 'weight_kg' argument
```

---

Анализ выполнен моделью: GPT-4o
